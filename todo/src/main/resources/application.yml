server:
  port: 8080
spring:
    datasource:
      url: jdbc:mysql://localhost:3306/db
      username: admin
      password: admin

    jpa:
      hibernate:
        ddl-auto: update
        show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQLDialect


